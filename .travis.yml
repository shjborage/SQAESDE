language: objective-c

before_install:
    - gem update
    - gem install cocoapods
    - brew update
    - brew install xctool

script:
    - cd Demo
    - pod install
    - xctool clean build test -workspace Demo.xcworkspace -scheme DemoTests -sdk iphonesimulator6.0
  